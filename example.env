# .env 템플릿 — 이 파일을 .env로 복사한 뒤 값을 채우세요:
#   cp example.env .env
# 기본 LLM은 로컬 LM Studio(localhost:1234)라서 API 키가 필요 없다.
# 아래 키는 config.py에서 LLM_PROVIDER="groq"(폴백)로 바꿀 때만 쓴다.
# Groq API 키 발급: https://console.groq.com/keys
GROQ_API_KEY=gsk_your_key_here
